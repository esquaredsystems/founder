_site/
.sass-cache/
.jekyll-cache/
.jekyll-metadata
Gemfile.lock
vendor/
.DS_Store

# Scratch folder used when a file has to be moved aside rather than deleted.
# Safe to remove entirely.
_to_delete/
